<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.MyIntroduction" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyIntroduction" parent="Base.Theme.MyIntroduction" />

    <style name="Theme.MyIntroduction.Fullscreen" parent="Theme.MyIntroduction">
        <item name="android:actionBarStyle">
            @style/Widget.Theme.MyIntroduction.ActionBar.Fullscreen
        </item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.MyIntroduction.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>